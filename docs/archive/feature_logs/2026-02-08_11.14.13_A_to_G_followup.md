# 2026-02-08 - 11.14.13 A~G Follow-up Stabilization

## Background
- Scope remains the same: continue only within `11.14.13.A ~ 11.14.13.G`.
- Do **not** start `H/I` in this round.

## What was finalized
- A (Trace / Raw Event)
  - Stabilized E2E verification for same-session trace toggle.
  - Added deterministic selector path (`sidebar-nav-settings`, `settings-trace-raw-toggle`, `settings-cancel-btn`).
  - Added event counter assertion (`agent-log-event-count`) to verify no new raw events after toggle off.

- B (LLM endpoint rotation)
  - Repaired broken `backend/tests/test_llm_rotation.py`.
  - Added/finished tests for:
    - single endpoint happy path,
    - weighted round-robin order,
    - failover to backup,
    - cooldown recovery,
    - explainable error when all attempts fail.

- C (Explainable trace UX)
  - Confirmed AgentLogPanel action/reason/result summary path in current implementation.
  - Added stable UI test hook for event count in console footer.

- D (Workbench first-loop)
  - Removed unintended dashboard jump side effect when clicking sidebar Workbench.
  - Added stable settings nav test id in Sidebar.
  - E2E chain now passes: `workbench -> ask news -> chat report mode -> citation visible`.

- E (Report index / replay)
  - Added API test coverage for `date_from/date_to/tag` filters.
  - Added citation source_id normalization consistency assertions (`source_id`, `source_id_original`, `source_id_consistent`).

- F (Session isolation / security)
  - Added cross-session reference isolation test (A session context cannot be resolved by B session).
  - Added RAG collection naming normalization test for session-shaped keys.

## Validation
- `pytest -q backend/tests` -> `337 passed, 8 skipped`
- `pytest -q tests/retrieval_eval/test_retrieval_eval_runner.py` -> `6 passed`
- `python tests/retrieval_eval/run_retrieval_eval.py --gate` -> `PASS`
- `npm run build --prefix frontend` -> `success`
- `npm run test:e2e --prefix frontend` -> `10 passed`

## Scope boundary kept
- `11.14.13.H` and `11.14.13.I` remain untouched in this round.
