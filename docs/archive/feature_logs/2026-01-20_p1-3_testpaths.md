# 2026-01-20 P1-3 测试路径统一

## 目标
- pytest 仅收集 `backend/tests`，避免混杂 legacy 测试路径。

## 变更范围
- `pytest.ini`
  - `testpaths` 仅保留 `backend/tests`。
  - `test/` 加入 `norecursedirs`，标记为 legacy。
- 文档同步：README 与 `docs/01-05` 说明统一测试入口。

## 测试
- `pytest --collect-only -q`

## 备注
- legacy 测试仍保留在 `test/` 目录，必要时可手动执行。
