# 2026-02-07 — 11.14.13 A~G 实施收口

## 背景
- 按当前迭代要求执行 `docs/06_LANGGRAPH_REFACTOR_GUIDE.md` 的 `11.14.13.A ~ 11.14.13.G`。
- 执行边界：做到 `G 发布门禁流水线（CI/CD）` 即停止，`H/I` 暂不启动。

## 实施摘要
- A（Trace/Raw 开关）：完成后端全局开关 + 请求级 override + 前端设置透传与持久化。
- B（LLM 多 endpoint）：完成 `llm_endpoints[]` 配置、`openai_compatible` 主命名与 `gemini_proxy` 兼容、轮换/冷却/恢复逻辑。
- C（可解释 Trace）：补齐 `input_state/input_sources/decision_summary/status_reason`，前端 ThinkingProcess 优先展示可读摘要。
- D（Workbench 首发）：落地 `/workbench`、最新研报/市场快讯/今日任务卡片，“问这条”可回跳聊天并带上下文。
- E（研报索引与回放）：新增研报索引、回放、收藏接口与对应服务层。
- F（会话隔离与脱敏）：规范 `tenant:user:thread` session_id，非法值拒绝；SSE 与错误输出做敏感字段脱敏。
- G（CI/CD 门禁）：重构为 6 阶段串联门禁（backend -> retrieval unit -> retrieval gate -> frontend build -> e2e -> artifacts）。

## 验收结果
- `pytest -q backend/tests` -> `327 passed, 8 skipped`
- `pytest -q tests/retrieval_eval/test_retrieval_eval_runner.py` -> `6 passed`
- `python tests/retrieval_eval/run_retrieval_eval.py --gate` -> `PASS`
- `npm run build --prefix frontend` -> `成功`
- `npm run test:e2e --prefix frontend` -> `7 passed`

## 文档与边界
- 已在 `docs/06_LANGGRAPH_REFACTOR_GUIDE.md` 的 `11.14.13` 勾选 A~G 已完成项，并保留未做子项为 TODO。
- 已在 `docs/06_LANGGRAPH_REFACTOR_GUIDE.md` 的 `13. Worklog` 追加本次落地证据。
- `11.14.13.H` 与 `11.14.13.I` 保持未执行状态，待下一轮上线演练阶段处理。
