# 2026-02-08 - 11.14.13.B UI 多 Endpoint 面板收口

## Scope
- Continue within `11.14.13.A ~ G` only.
- This round closes the remaining B-item: Settings multi-endpoint visual config panel.

## Delivered

### 1) Settings 多 endpoint 可视化编辑
- File: `frontend/src/components/SettingsModal.tsx`
- Added `llm_endpoints[]` UI editor with:
  - add/remove endpoint rows
  - editable fields: `name/provider/api_base/api_key/model/weight/enabled/cooldown_sec`
  - save-time normalization (trim + min constraints)

### 2) 配置兼容策略
- Save payload always includes normalized `llm_endpoints[]`.
- When endpoints exist, first endpoint is backfilled to legacy fields:
  - `llm_provider/llm_model/llm_api_base/llm_api_key`
- Load path keeps legacy compatibility via normalization:
  - if `llm_endpoints[]` empty but legacy fields exist, auto-hydrate one editable endpoint row.

### 3) Provider 命名对齐
- UI provider primary naming uses `openai_compatible`.
- `gemini_proxy` is preserved as alias option for compatibility.

### 4) E2E 覆盖
- File: `frontend/e2e/report-button.spec.ts`
- Added case: `Settings can add multi-endpoint rotation and save llm_endpoints`
  - verifies `/api/config` POST payload includes two endpoints
  - verifies weight/cooldown/enabled/model/provider fields
  - verifies legacy backfill from first endpoint

## Validation
- `npm run build --prefix frontend` -> success
- `npm run test:e2e --prefix frontend` -> 13 passed

## Notes
- This completes the previously pending B-UI item under `11.14.13.A~G`.
- H/I scope remains intentionally untouched.

